#include <iostream>
#include <cstdlib>

using namespace std;

int main(){
    /*==============================> ARITMÉTICA DE INTEIROS <====================

    int n=-10;
    int numerador, denominador;
    div_t res;

    numerador=10;
    denominador=3;
    res=div(numerador, denominador);

    cout << abs(n) << endl;
    cout << res.quot << " " <<res.rem << endl;
    --------------------------------------------------------------------------------
    abs > Valor absoluto (função)
    div > Divisão integral (função)
    labs > Valor absoluto (função)
    ldiv > Divisão integral (função)
    llabs > Valor absoluto (função)
    lldiv > Divisão integral (função)
    ===============================================================================*/


    return 0;
}
