#include <iostream>

using namespace std;

int main(){

    /*=============================> FUNÇÕES <===============================

    ------------------------------- Copiando --------------------------------

    memcpy > Copiar bloco de memória (função)
    memmove > Mover bloco de memória (função)
    strcpy > Copiar string (função)
    strncpy > Copiar um determinado numero de caracteres da string (função)

    ------------------------------- Concatenação --------------------------------

    strcat > Concatenar strings (função)
    strncat > Concatena um determinado numero de caracteres da string (função)


    ------------------------------- Comparação --------------------------------

    memcmp > Compare dois blocos de memória (função)
    strcmp > Compare duas strings (função)
    strcoll > Compare duas strings usando com base na sua localização (função)
    strncmp > Compare um determinado numero de caracteres das duas strings (função)
    strxfrm > Transforme a string usando a localidade (função)


    ------------------------------- Procurando --------------------------------

    memchr > Localize o caractere no bloco de memória (função)
    strchr > Localize a primeira ocorrência do caractere na string (função)
    strcspn > Localiza os caracteres que estam na chave de pesquisa(função)
    strpbrk > Localiza caracteres dentro da lista - MAS - É MUITO UTIL VEJA (função)
    strrchr > Localize a última ocorrência do caractere na string (função)
    strspn > Recebe uma str1 e str2, retorna quantos dos caracteres da str1 fazem parte da str2 (função)
    strstr > Localizar uma ocorencia de str2 em str1, retorna um ponteiro naquela posição(função)
    strtok > Divida a string em pedaçõs (função)

    ------------------------------- Outros --------------------------------

    memset > Preencher bloco de memória (função)
    strerror > Obter ponteiro para string de mensagem de erro (função)
    strlen > Obter comprimento de string (função)

    ========================================================================*/

    return 0;
}
